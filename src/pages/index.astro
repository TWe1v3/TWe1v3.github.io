---
import BaseLayout from '../layouts/BaseLayout.astro';

const postModules = import.meta.glob('./posts/*.{astro,md}', { eager: true });
const posts = Object.values(postModules)
  .map((mod) => mod.postMeta ?? mod.frontmatter)
  .filter(Boolean)
  .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime());
---
<BaseLayout title="TWe1v3" description="能力沉淀、个人思考、自动化实验与安全研究。">
  <div class="shell">
    <nav class="site-nav">
      <a class="brand" href="/">
        <span class="brand-mark"></span>
        <span>TWe1v3</span>
      </a>
      <div class="nav-note">A restrained public notebook for refined work.</div>
    </nav>

    <div class="divider"></div>

    <header class="home-intro">
      <div>
        <div class="kicker">Public writing</div>
        <h1>把判断、作品和系统感，慢慢写成可见的东西。</h1>
        <p class="lede">这里放的不是所有草稿，而是筛过之后值得公开的内容：安全研究、自动化实验、AI 工作流、以及一些关于 operator discipline 的长期笔记。目标不是堆信息密度，而是让每一篇都像真的作品。</p>
      </div>
      <div class="intro-side">
        <div class="intro-note">
          <div class="meta-kicker">Focus</div>
          <p>AI agent 边界、自动化系统、安全研究、以及个人工作台的可持续设计。</p>
        </div>
        <div class="intro-note">
          <div class="meta-kicker">Rule</div>
          <p>原始素材先留在私域；只有结构够清楚、观点够稳、能代表水准的内容，才会进入这里。</p>
        </div>
      </div>
    </header>

    <section class="section">
      <div class="section-head">
        <div>
          <div class="section-kicker">Latest</div>
          <h2>Recent writing</h2>
        </div>
        <p class="section-copy">参考了 GitHub 上做得好的 Astro 博客方向后，这里会更偏 typography-first：少装饰、强排版、重阅读，不做廉价模板感。</p>
      </div>

      <div class="post-list">
        {posts.map((post) => (
          <a class="post-row" href={post.href}>
            <div class="post-date">{post.date}</div>
            <div>
              <h3>{post.title}</h3>
              <p>{post.desc}</p>
              {post.tags?.length ? (
                <div class="pill-row">
                  {post.tags.map((tag) => <span class="pill">{tag}</span>)}
                </div>
              ) : null}
            </div>
            <div class="post-link-mark">↗</div>
          </a>
        ))}
      </div>
    </section>

    <section class="about-block">
      <div class="section-kicker">Why this exists</div>
      <p>这站应该像一个有判断的人写出来的技术博客，而不是“我也搭了个站”的完成任务页面。所以后面我会继续清理语言、减少装饰、增强文章页的可读性，把它往更成熟的个人写作者站点推进。</p>
    </section>

    <footer class="site-footer">TWe1v3 · Built with Astro · Taste first, then automation</footer>
  </div>
</BaseLayout>
