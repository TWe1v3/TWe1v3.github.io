---
import PostLayout from '../../layouts/PostLayout.astro';

export const postMeta = {
  title: "别把审计日志当事后记录：对 Agent 系统来说，它本身就是安全边界",
  date: "2026-04-13",
  desc: "很多团队做 agent 时，会把“权限控制”和“审计日志”拆成两件事：前者负责拦，后者负责事后看。但这两天重新梳理本地的 agent、browser、tool 调用链后，我越来越确定，这个拆法太乐观了。对 agent 系统来说，审计不是事故",
  href: "/posts/agent",
  tags: ["Blog","Automation"]
};
---
<PostLayout title={postMeta.title} date={postMeta.date} description={postMeta.desc} tags={postMeta.tags}>
  <p>很多团队做 agent 时，会把“权限控制”和“审计日志”拆成两件事：前者负责拦，后者负责事后看。但这两天重新梳理本地的 agent、browser、tool 调用链后，我越来越确定，这个拆法太乐观了。对 agent 系统来说，审计不是事故之后的录音带，它本身就是控制面的一部分。</p>
  <p>很多系统会记录“授权通过了”“用户点了确认”“本次会话有权限”，听起来像是安全措施，但这些记录往往没有把真正关键的东西绑在一起：是谁发起的、调用了哪个工具、作用到哪个对象、具体执行了什么 payload、最终结果是什么。如果日志里只有“准许过”，却没有“准许的是这个对象上的这个动作，并且结果已回读确认”，那所谓审批很容易退化成一种氛围，而不是边界。</p>
  <p>这件事在 agent 场景里尤其明显。因为 agent 不是单次 API 调用，而是一串带上下文的动作：读页面、切标签、调工具、写文档、发消息、跑定时任务。只要对象作用域、执行边界和结果回读没有被逐次固定下来，系统就会偷偷滑向“会话级信任”。而会话级信任几乎总是过宽：它默认当前上下文是连续可靠的，默认下一步操作和上一步确认属于同一个风险面，也默认中断、恢复、重试不会带来对象漂移。</p>
  <p>最近本地几条材料刚好能拼成一个更清楚的判断。第一，浏览器侧已经反复说明：公开网页读取、登录态 attach、后台写操作，本来就不是一个等级的动作，不该混在一个长寿命上下文里。第二，工具侧也越来越明确：单纯“授权过”不够，应该把 actor、tool、target object、action、approval state、result read-back 绑定成一条最小可核验链。第三，执行可靠性问题其实也属于这个边界：如果任务做到一半被等待、socket、auth 或上下文切换打断，却没有留下明确的下一条命令、预期对象和完成证据，系统就会在恢复时重新猜上下文，这本身就是风险。</p>
  <p>所以，我现在更倾向于把一套可用的 agent 安全设计写得更朴素一点：先拆开读和写，拆开公开内容与登录态，拆开低风险推进与高风险审批；再要求每个高风险动作都带上对象预览、payload 预览和执行后的结果回读。这样做并不炫，但它会把“我大概同意过”变成“我确实同意这个动作作用到这个对象，并且结果符合预期”。真正可靠的 agent，不只是会在危险前停下来，还得能在事后留下足够具体的证据，让人知道它到底做了什么。</p>
</PostLayout>
